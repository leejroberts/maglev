source 'https://rubygems.org'
ruby '3.0.2'
git_source(:github) { |repo| "https://github.com/#{repo}.git" }

# Specify your gem's dependencies in maglev.gemspec.
gemspec

group :development do
  gem 'sqlite3'
end

# To use a debugger
# gem 'byebug', group: [:development, :test]
